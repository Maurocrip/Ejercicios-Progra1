/*
 * array.h
 *
 *  Created on: 17 may. 2022
 *      Author: Usuario
 */

/**
 * @fn void sortPositivosNegativos(int[], int)
 * @brief ordena los numeros positivos de array de mayor a menor los numeros de un array.
 *
 * @param vector: nombre del array que se quiera ordenar.
 * @param tam: tamñao de array.
 */
void sortPositivosNegativos(int vector[], int tam);

/**
 * @fn void mostrarArriveInt(int[], int, char*)
 * @brief muestra todos los numeros de un array.
 *
 * @param vector: array que se quiera mostrar.
 * @param indice: el tamaño del array.
 * @param mensaje: el mensaje que se quiera mostrar antes que se muestren los numeros del array.
 */
void mostrarArriveInt(int vector[], int indice, char* mensaje);

/**
 * @fn void PedirArriveInt(int[], int, char*)
 * @briefle pide los datos al usuario para llenar un array de tipo int.
 *
 * @param vector: array donde se guarden los datos.
 * @param indice: el tamaño del array.
 * @param mensaje: el mensaje que se quiera mostrar antes que se pidan los datos.
 */
void PedirArriveInt(int vector[], int indice, char* mensaje);



#ifndef ARRAY_H_
#define ARRAY_H_



#endif /* ARRAY_H_ */
